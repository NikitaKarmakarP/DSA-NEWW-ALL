#include <iostream>
using namespace std;

int fact[100005];
void preComputerFactorial(){
    fact[0] = 1;
    int[ans] = 1;
    for(int i = 1; i<=100005; i++){
        ans = ans * i;
        fact[i] = ans;
    }
}

int nCr (int n, int r){
    return fact[n] / fact[r] * fact[n-r]);
}

void testCaseNcR(){
    preComputerFactorial()
    int t;
    cin >> t;
    while(t--){
        int n, r;
        cin>>n>>r;
        cout<<ncr(n,r)<<endl;
    }
}

int main() {
    cout << "GfG";
    return 0;
}
